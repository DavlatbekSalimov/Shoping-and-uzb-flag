
import 'package:flutter/material.dart';

class Chiziq extends StatelessWidget {
  const Chiziq({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Padding(
            padding: EdgeInsets.only(left: 19, right: 10),
            child: Divider(
              color: Colors.black,
            ),
          );
  }
}

class Ajratuvchi extends StatelessWidget {
  const Ajratuvchi({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10,);
  }
}